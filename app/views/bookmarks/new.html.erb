<h1>New bookmark to</h1>

<%= simple_form_for ([@list, @bookmark]) do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Add bookmark", class: "btn btn-secondary" %>
<% end %>

<%# <%= link_to 'Back', list_bookmark_path %>